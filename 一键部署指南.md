# 一键部署指南 · 三种方案 · 从注册到公网可用

> 你的账号服务代码已准备好，只需要选一个云平台，把代码推上去就行。
> 每种方案都有免费额度，部署后得到一个公网 HTTPS 地址，
> 所有用户可以随时随地注册登录扫码支付。

## 方案对比（推荐排序）

| | Railway | Fly.io | 阿里云轻量 |
|---|---|---|---|
| 费用 | 免费额度 $5/月 | 免费额度 3 个共享 VM | ¥30-60/月 |
| 注册门槛 | GitHub 账号即可 | 信用卡（不扣费） | 实名认证 |
| 国内访问速度 | 一般 | 香港/新加坡节点快 | 最快 |
| 部署难度 | ⭐ 最简单 | ⭐⭐ | ⭐⭐⭐ |
| 适合阶段 | 测试/小量用户 | 测试/正式 | 正式运营 |
| 免运维 | ✅ 全托管 | ✅ 全托管 | ❌ 需自己维护 |

## 方案 A：Railway（推荐先做这个，5 分钟上线）

### 前置
- 注册 GitHub 账号（github.com，免费）
- 注册 Railway（railway.app，用 GitHub 账号直接登录）

### 步骤（全部在浏览器操作，无需安装任何东西）
1. **推送代码到 GitHub**：
   - 在 GitHub 新建一个**私有仓库**（如 `xiaomada-server`）
   - 把交付包里 `6-账号服务器/server/` 目录的内容上传到这个仓库
     （GitHub 网页端就能上传：Add file → Upload files → 拖入 bin/ 和 Dockerfile）
2. **Railway 部署**：
   - railway.app → New Project → Deploy from GitHub repo → 选刚建的仓库
   - Railway 检测到 Dockerfile 自动构建
   - Settings → Networking → Generate Domain → 得到地址如
     `https://xiaomada-api-production.up.railway.app`
3. **验证**：浏览器打开 `https://你的地址/health` 看到 JSON 即成功
4. **测试**：`curl https://你的地址/auth/register -X POST -H "Content-Type:application/json" -d '{"account":"13800138000","password":"test12345"}'`

### 后续构建 App
```bash
flutter build apk --release \
  --dart-define=XMD_API_BASE=https://xiaomada-api-production.up.railway.app \
  --split-per-abi --obfuscate --split-debug-info=build/symbols
```

## 方案 B：Fly.io（亚洲节点快）

```bash
# 安装 flyctl（Windows PowerShell）：
curl https://fly.io/install.ps1 | iex

# 登录（需要先注册 fly.io，绑信用卡但不扣费）
fly auth login

# 部署
cd server
fly launch          # 自动检测 fly.toml，一路回车
fly status          # 查看公网地址 https://xiaomada-api.fly.dev
```

## 方案 C：阿里云轻量（国内用户访问最快）

1. 购买：aliyun.com → 轻量应用服务器 → 最低配（2核2G，¥30-60/月）→ 地域选离你近的
2. SSH 连上后执行：
```bash
curl -fsSL https://dart.dev/get-dart | sh
mkdir -p /opt/xiaomada && cd /opt/xiaomada
# 把 server 目录上传到这里（scp 或宝塔面板文件管理）
cd server/bin
nohup dart server.dart 8080 > /var/log/xiaomada.log 2>&1 &
```
3. 防火墙开放 8080 端口
4. （推荐）装 Nginx + Let's Encrypt HTTPS：
```bash
apt install nginx certbot python3-certbot-nginx
# Nginx 配置 /etc/nginx/sites-available/xiaomada:
# server { listen 443 ssl; server_name api.你的域名.com;
#          location / { proxy_pass http://127.0.0.1:8080; } }
certbot --nginx -d api.你的域名.com
```
5. 需要一个域名（¥10-50/年，阿里云/腾讯云买）→ DNS 解析到 VPS IP

## 部署后管理

### 管理后台（网页版）
浏览器打开 `https://你的地址/admin`，粘贴密钥（data/secret.key 文件内容），可以：
- 配置/更换四档收款码（直接上传本地截图）
- 查看全部订单并一键确认收款
- 查看全部账号与会员状态
- 重置用户密码

### 生成激活码
```bash
# SSH 到服务器
cd /opt/xiaomada/server/bin
dart admin.dart gen year 10    # 生成 10 张年卡
dart admin.dart list           # 查看账号
```

### 构建 App（指向线上服务器）
```bash
flutter build apk --release \
  --dart-define=XMD_API_BASE=https://你的服务器地址 \
  --split-per-abi --obfuscate --split-debug-info=build/symbols
```

## 常见问题

**Q：免费额度够用多久？**
Railway $5/月免费额度足够几千个用户的轻量 API 服务。用户量大后再付费（约 ¥20/月）。

**Q：用户数据安全吗？**
服务器只存邮箱/手机号+加密密码+会员期限。儿童数据永远只在用户手机上。

**Q：我能随时换服务器吗？**
能。整个数据就是 data/ 目录里的几个 JSON 文件，拷走就能迁移。
